# ---- fastfetch (kitty only) ----
if [[ -o interactive ]] \
  && [[ "$TERM" == "xterm-kitty" ]] \
  && command -v fastfetch >/dev/null 2>&1 \
  && [[ -z "$SSH_CONNECTION" ]] && [[ -z "$TMUX" ]] \
  && [[ -z "$FASTFETCH_SHOWN" ]]; then
  export FASTFETCH_SHOWN=1
  fastfetch
fi
